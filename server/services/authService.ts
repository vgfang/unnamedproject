import jwt from "jsonwebtoken";

import * as TokenService from "./tokenService";
import { type Token, TokenType } from "../models/token";

export const storeJWTAuthToken = () => {
  const token = {};

  // TokenService.upsertToken()
};
